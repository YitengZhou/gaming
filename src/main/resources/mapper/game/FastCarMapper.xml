<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhouyiteng.gambling.dao.game.FastCarMapper">
    <insert id="createNewRace">
        insert into gambling.fast_car
        (first, second, third, fourth, fifth, sixth, seventh, eighth, ninth, tenth, generate_type, remark,
         create_time, update_time)
        values
        (#{model.first}, #{model.second}, #{model.third}, #{model.fourth}, #{model.fifth}, #{model.sixth},
         #{model.seventh}, #{model.eighth}, #{model.ninth}, #{model.tenth}, #{model.generateType}, #{model.remark},
         now(), now())
    </insert>

    <select id="getTotalCount" resultType="java.lang.Long">
        select count(1) from gambling.fast_car where 1=1
    </select>

    <select id="getPageList" resultType="com.zhouyiteng.gambling.model.game.FastCarModel">
        select * from gambling.fast_car where 1=1
        order by create_time desc limit #{pageStart},#{pageEnd}
    </select>
</mapper>