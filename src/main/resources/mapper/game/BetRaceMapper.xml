<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhouyiteng.gambling.dao.game.BetRaceMapper">
    <insert id="addBetRace">
        insert into gambling.bet_race
        (create_time, race_id, user_id, total_money, sum3)
        values
        (now(), #{model.raceId}, #{model.userId}, #{model.totalMoney}, #{model.sum3})
    </insert>
</mapper>